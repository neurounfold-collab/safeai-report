# Admin Access Credentials
# Copy to .env or .env.local and replace with production secrets.
# Never commit real credentials — .env / .env.local are gitignored.

VITE_ADMIN_USERNAME=admin_demo
VITE_ADMIN_PASSWORD=change_this_password
