# Administrative gate credentials (Vite build-time)
# Copy to .env.local and override. Quote passwords that contain # or $.
VITE_ADMIN_USERNAME=admin_a4i_master
VITE_ADMIN_PASSWORD="OIARF#2026!Secured@WaqfLedger$Master"

# Server-only WaqfLedger sealing (Vercel / api/grade-exam.js) — never prefix with VITE_
WAQFLEDGER_ENDPOINT=https://waqfledger-api.vercel.app/api/log-compliance
WAQFLEDGER_API_SECRET=
